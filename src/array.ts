const names: string[] = ["Alice", "Bob", "Eve"];
const ages: number[] = [20, 21, 22];

const user: [string, number] = ["Alice", 20];

ages.map((age) => age.toFixed);
names.map((name) => name.toUpperCase());
